# MongoDB Atlas Setup

1. Create a free cluster in Atlas.
2. Add database user and strong password.
3. Add IP whitelist entry for server IP.
4. Copy connection string into `.env` as `MONGO_URI`.
5. Enable backups in Atlas (recommended for production).
